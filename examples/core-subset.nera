// A straight-line example of the frozen frontend prototype's source subset.
// Current frontend acceptance is NOT historical Lean certification.
fn main() -> u64 {
    let p = alloc<u64>(1);
    *p = 20 + 22;
    let answer = *p;
    free(p);
    return answer;
}
